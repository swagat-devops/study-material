$ docker run --name oracle -v /volume/oracle:/app/data --network my-network -e MYSQL_DATABASE=mydb -e MYSQL_ROOT_PASSWORD=oracle123 -e MYSQL_USER=swagat -e MYSQL_PASSWORD=Swagat@1999  -p 3333:3306 -d mysql:8.0

MYSQL_USER, MYSQL_PASSWORD,MYSQL_DATABASE

$ docker run -it --network some-network --rm mysql mysql -h oracle -u root -p


$ docker run --name some-mysql -v /my/custom:/etc/mysql/conf.d -e MYSQL_ROOT_PASSWORD=my-secret-pw -d mysql:tag


docker run -d --name springboot-backend --network my-network -e MYSQL_DB_DATASOURCE=jdbc:mysql://oracle:3306/mydb -e MYSQL_DB_USERNAME=root -e MYSQL_DB_PASSWORD=oracle123 -p 8001:8080 backend:v4

docker run -d --name frontend --network my-network -p 8000:8080 swagatdevops/frontend:v2


----------------
server {
    listen 80;

    # Serve Angular app
    root /usr/share/nginx/html;
    index index.html;
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Proxy API calls to backend container
    location /api/ {
        proxy_pass http://springboot-backend:8080/api/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
